import React from 'react'
import {AppBar, Toolbar} from '@material-ui/core'

function Header() {
    return (
        <AppBar>
            <Toolbar>
                My Toolbar
            </Toolbar>
        </AppBar>
    )
}

export default Header
